{
  "name": "cryptforge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "doctor:tauri": "node scripts/doctor-tauri.mjs",
    "verify:frontend": "npm run build",
    "verify:rust": "npm run doctor:tauri && cargo test --manifest-path src-tauri/Cargo.toml",
    "verify": "npm run verify:frontend && npm run verify:rust"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.3.0",
    "@tauri-apps/plugin-shell": "^2.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
